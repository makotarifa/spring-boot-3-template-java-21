version: '3.8'

services:
  db:
    image: postgres:14-alpine
    container_name: template-db
    environment:
      POSTGRES_USER: template
      POSTGRES_PASSWORD: template
      POSTGRES_DB: template
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U template -d template"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:8.3
    container_name: template-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8081:80"
    depends_on:
      - db

  app-runner:
    build:
      context: ../..
      dockerfile: docker/app/Dockerfile
    image: makotarifa/spring-boot-3-template-java-21:latest
    container_name: template-app-runner
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/template
      - SPRING_DATASOURCE_USERNAME=template
      - SPRING_DATASOURCE_PASSWORD=template
      - SPRING_SECURITY_OAUTH2_CLIENT_ID=demo-client
      - SPRING_SECURITY_OAUTH2_CLIENT_SECRET=demo-secret
      - SPRING_SECURITY_JWK_URI=http://app-runner:8080/.well-known/jwks.json
      - SERVER_PORT=${SERVER_PORT:-8080}
      - HOST_PORT=${HOST_PORT:-8080}
    ports:
      - "${HOST_PORT:-8080}:${SERVER_PORT:-8080}"
    depends_on:
      - db

volumes:
  db_data:
    driver: local

networks:
  default:
    name: template_network
